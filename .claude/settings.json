{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Enable sandbox in Claude Code with /sandbox (choose auto-allow or regular permissions). Project defaults: .claude/settings.json. Personal overrides: .claude/settings.local.json (gitignored).",
  "sandbox": {
    "enabled": true,
    "autoAllowBashIfSandboxed": true,
    "network": {
      "allowedDomains": [
        "registry.npmjs.org",
        "registry.yarnpkg.com",
        "*.github.com",
        "*.githubusercontent.com",
        "*.googleapis.com"
      ]
    }
  }
}
